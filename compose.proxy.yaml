services:
  app:
    image: pi-compressor:${PI_RELEASE:?Set PI_RELEASE to an exact release tag}
    networks:
      proxy:
        aliases: [pi-compressor]

networks:
  proxy:
    external: true
    name: proxy_net
